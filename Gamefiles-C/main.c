//
// Created by Werwolf2 on 19.05.2021.
//

#include <raylib.h>       //rayPlayAudio
#include "windowsray.h"

int main(void)
{
    /* When (->) own GameLoop in function
     * When (__x__) Files (type (x)) loaded
     *
     * load(__settings__)
     *
     * InitWindow(FULLSCREEN)
     *
     * PlayIntro(RaylibIcon, JKAIcon, (42?))
     *
     *
     * Start_Main_Menu()
     *      Submenus with different Background Images but same button layout
     *
     * -> custom game and campaign(Own Layout)
     *      ->actual game called by them(when savegames also ->(loading files (loading screen)))
     *
     *  structs:
     *      Tile
     *      Units
     *
     */

}